[project]
name = "meshcore-ping-bot-ble"
version = "0.2.0"
description = "BLE channel ping bot for MeshCore companion radios"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "meshcore>=2.3.8,<3",
]
